# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PhotoBrowser' do
  use_frameworks!
  pod "SDWebImage"
  pod "SVProgressHUD"
  pod "Masonry”
end
